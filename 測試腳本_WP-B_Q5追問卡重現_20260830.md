# WP-B Q5(寬度追問卡)重現步驟

給監督自己跑、自己截圖、自己判讀用。我(claude-code-7e)驗證時的環境：macOS + 這個沙盒內建的
Chromium-based Browser pane（不是真實Chrome）。可能不一樣的地方：如果你是用真實瀏覽器開發者工具
跑，`window.fetch`覆寫、`DataTransfer`建構、`dispatchEvent`這三個API在Chrome/Safari/Firefox
行為應該一致，沒有已知的環境依賴；唯一要注意的是`pro.html`本身用了`lib-edit3d-photo2tub.js`
(commit 8b3d6f0)，跑之前確認本機檔案是這個版本(`git log -1 -- lib-edit3d-photo2tub.js`)。

## 1. 起本機static server

```bash
cd "/Users/lyric/Documents/Claude Code/Kreiner/Australia/客製化浴缸三版本平台/bathe-atelier"
python3 -m http.server 8791
```

（用哪個port不重要，8791只是我測試時剛好用的，沒有跟其他服務衝突就好）

## 2. 開頁面

瀏覽器開 `http://localhost:8791/pro.html`
（**不是**`photo2tub-app.html`——那頁是獨立單檔內嵌腳本，沒有引用`lib-edit3d-photo2tub.js`，
跑這支腳本在那頁上不會有效果，我第一次測試時踩過這個坑）

## 3. 開發者工具Console，貼上以下其中一段（各自一行/一段就能切換）

### Case A：`W_source: 'low_confidence'`（預期：Q4+Q5兩排都出現）

```js
window.__stubWSource = 'low_confidence';
window.fetch = async (url, opts) => {
  const u = String(url);
  if (u.includes('/reconstruct-submit')) return { ok: true, status: 200, json: async () => ({job_id: 'stub123'}) };
  if (u.includes('/reconstruct-status/')) return { ok: true, status: 200, json: async () => ({
    status: 'done', elapsed_sec: 1.2,
    spec: {
      '設計參數': {'外部長度_mm':1700,'外部寬度_mm':780,'shape_code':'rect','手繪俯視輪廓_normalized':null,'寬度區間_mm':[700,860],'缸緣高度_前端_mm':580},
      'field_confidence': {'dims':'low'},
      'H_source': 'default_no_side_view',
      'W_source': window.__stubWSource,
    },
    messages: ['[stub] W_source=' + window.__stubWSource + ' 測試訊息'],
  })};
  return new Response('{}', {status:404});
};
const inp = document.getElementById('photo2tubFiles');
const file = new File([new Uint8Array([137,80,78,71,13,10,26,10])], 'stub.png', {type:'image/png'});
const dt = new DataTransfer();
dt.items.add(file);
inp.files = dt.files;
inp.dispatchEvent(new Event('change', {bubbles:true}));
```

等約4秒（stub的status輪詢固定3秒一次），畫面上的banner應該出現：
- 一句「⚠ Please manually confirm: dims (low confidence).」
- 追問卡：Q4「About how tall is it...」+ Q5「The width shown may not be reliable — about how wide is it...」兩排，各6顆按鈕（5個區間+1個unsure）

### Case B：`W_source: 'trusted'`（預期：只有Q4，Q5那排完全不存在）

先把上面的卡片移除、換一個新的假檔案觸發第二次（同一個input再fire一次change也可以，
不影響banner重新渲染）：

```js
window.__stubWSource = 'trusted';
document.getElementById('p2tHintCard') && document.getElementById('p2tHintCard').remove();
const inp2 = document.getElementById('photo2tubFiles');
const file2 = new File([new Uint8Array([137,80,78,71,13,10,26,10])], 'stub2.png', {type:'image/png'});
const dt2 = new DataTransfer();
dt2.items.add(file2);
inp2.files = dt2.files;
inp2.dispatchEvent(new Event('change', {bubbles:true}));
```

（`window.fetch`已經在Case A那段覆寫過、且讀`window.__stubWSource`活變數，這裡只要改那個變數
再重新觸發一次change即可，不用重貼整段fetch stub）

等約4秒，這次banner應該**只有Q4那一排，完全沒有Q5**。

## 4. 判讀重點

- Case A：`document.getElementById('p2tHintCard').outerHTML`裡應該同時看到`data-q="4"`跟
  `data-q="5"`兩組按鈕
- Case B：同一個查詢應該只看到`data-q="4"`，完全沒有`data-q="5"`
- 兩個case用同一支`window.fetch` stub、同一個`photo2tubFiles` input，只差`W_source`這一個
  欄位的值——如果畫面上的差異跟這個欄位以外的東西有關，那代表我的實作有問題，不是預期行為

## 附註

- 這支只驗證「渲染這一半」（前端讀`W_source`→決定顯示哪些追問卡題目），不驗證「後端算出
  `W_source`」那一半——那一半我用單元測試(`compute_W_source()`4組case)覆蓋過，見commit
  c6cd711的commit message。
- Q5選了之後(`applyHint`裡`q==='5'`分支)會直接覆寫`外部寬度_mm`並清空`寬度區間_mm`，這支
  重現腳本沒有涵蓋到「選按鈕之後」那段行為，只涵蓋「卡片出不出現」——如果你也想測選按鈕後的
  效果，可以在Case A跑完之後手動點擊某個Q5按鈕，再查`p2tLastData.spec['設計參數']['外部寬度_mm']`
  有沒有變成對應的值。
